name: "Check Manifest"

on:
  schedule:
    - cron: '*/3 * * * *'

jobs:
  check:
    runs-on: ubuntu-latest
    steps:
      - run: pip3 install check-manifest scipy cython & check-mainfest -v
        shell: bash
      - run: pip3 freeze
        shell: bash